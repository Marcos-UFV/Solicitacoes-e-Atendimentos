<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/history_card_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="@color/SOLICITA_green"
    app:strokeWidth="1dp"
    app:strokeColor="@color/SOLICITA_green" >

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!--Status Icon -->
        <ImageView
            android:id="@+id/concluded_history_card_status_icon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentTop="true"
            android:layout_alignParentEnd="true"
            android:contentDescription="Ícone de status"
            android:src="@drawable/check_24"
            android:tint="#FFFFFF" />

        <!-- Title -->
        <TextView
            android:id="@+id/concluded_history_card_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Título do Card"
            android:textStyle="bold"
            android:textSize="16sp"
            android:layout_alignParentStart="true"
            android:layout_marginEnd="32dp"
            android:layout_alignTop="@id/concluded_history_card_status_icon"
            android:textColor="#FFFFFF" />

        <!-- Date -->
        <TextView
            android:id="@+id/concluded_history_card_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="xx/xx/xxxx"
            android:layout_below="@id/concluded_history_card_title"
            android:layout_alignParentStart="true"
            android:layout_marginTop="8dp"
            android:textColor="#FFFFFF" />

    </RelativeLayout>
</com.google.android.material.card.MaterialCardView>
